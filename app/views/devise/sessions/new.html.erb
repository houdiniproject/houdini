<%- # License: AGPL-3.0-or-later WITH WTO-AP-3.0-or-later
# Full license explanation at https://github.com/houdiniproject/houdini/blob/main/LICENSE -%>
<% content_for :title, t("login.header") %>
<% content_for :javascripts do %>
<%= javascript_packs_with_chunks_tag 'user-signin-bundle' %>
<% end %>
<% sign_in_common = JSON::parse(render('users/sign_in')) %>
<%= react_component('SignInPage', props: sign_in_common.merge(innerProps: { redirectUrl: session[:previous_url] || root_path })) %>