<%- # License: AGPL-3.0-or-later WITH Web-Template-Output-Additional-Permission-3.0-or-later -%>
<!DOCTYPE html>
<html>
<head>

	<title><%= yield(:title) || "Donate with #{Settings.general.name}" %></title>
	<script>
		var app = {
			nonprofit: <%=raw(@nonprofit.attributes.slice('brand_color', 'brand_font').to_json)%>
		}
	</script>
	<%= render 'layouts/stylesheets' %>
	
	<%= favicon_link_tag %>
	<%= csrf_meta_tags %>
	<meta name="Description" content="Intelligent fundraising for nonprofits.">

</head>

<body id='<%= yield :body_id %>' class='embedded-layout'>
<%= yield %>
<%= yield :javascripts %>
</body>

</html>
