<%- # License: AGPL-3.0-or-later WITH Web-Template-Output-Additional-Permission-3.0-or-later -%>
<li class="<%= (status == :temporarily_verified || status == :verified) ? 'is-done' : '' %>">
	<span class='checkbox-outer'><i class='icon-check-1'></i></span>
	<span class='name'>
		<% if status == :pending %>
			Your verification data has been submitted and is pending approval. It should complete within 24 hours.
		<% else %>
			Please complete the

			<% if status == :unverified %>
				<a href="<%= verification_nonprofits_stripe_account_url(@nonprofit, {return_location: 'payouts'})%>"> <strong>identity verification form for Stripe</strong></a>. Until this step is completed, you will <strong>not</strong> be able to accept payments on CommitChange.<% else %> identity verification form for Stripe.<% end %>
		<% end %>
	</span>
</li>


