# License: CC0-1.0
# grape validation errors require a very specific format for their translation messages. Why? *shrug*
en:
  grape:
    errors:
      messages:
        is_equal_to: "must be the same"
        less_than_or_equal: "must be less than or equal to %{value}"
        greater_than_or_equal: "must be greater than or equal to %{value}"